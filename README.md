# Тестирование API для сервиса "Stellar Burger"

## Описание проекта
Проект представляет собой набор автотестов для проверки эндпоинтов API сервиса "Stellar Burger". Тесты охватывают ключевые функциональные модули: создание пользователя, авторизацию и создание заказов. Проект демонстрирует профессиональный подход к автоматизации тестирования REST API.

## Объем выполненной работы

- **3 комплексных тестовых модуля**, охватывающих основные функциональные блоки API:
  - `test_create_user.py` (создание пользователя)
  - `test_login.py` (авторизация пользователя)
  - `test_create_order.py` (создание заказов)

- **5 вспомогательных модулей** для организации тестовой инфраструктуры:
  - Конфигурационные файлы (URL, статус-коды, тексты ответов)
  - Генераторы тестовых данных
  - Фикстуры для подготовки тестового окружения

## 🛠 Используемые технологии и библиотеки

### Основные фреймворки:
- **Pytest** - основной фреймворк для написания и запуска тестов
- **Allure** - создание детализированных и визуально привлекательных отчётов
- **Requests** - выполнение HTTP-запросов к API

## Применённые паттерны и подходы

### 1. **Разделение данных и логики**
- Тестовые данные вынесены в отдельные модули (`data/`)
- Генерация тестовых данных инкапсулирована в `helpers/helpers.py`
- Конфигурационные данные (URL, статус-коды) изолированы

### 2. **Page Object Pattern (адаптированный для API)**
- Каждый тестовый класс соответствует определённому эндпоинту
- Методы-хелперы для создания тестовых данных

### 3. **Фикстуры Pytest**
- Использование `conftest.py` для общих фикстур
- Фикстура `create_new_user` для переиспользования создания пользователя

### 4. **Параметризация тестов**
- Использование `@pytest.mark.parametrize` для тестирования разных наборов данных

### 5. **Allure-отчёты**
- Детализированные шаги тестов (`@allure.step`)

## ✅ Покрытие функциональности

### Создание пользователя:
- Успешное создание уникального пользователя
- Защита от дублирования пользователей
- Валидация обязательных полей
- Очистка тестовых данных после выполнения

### Авторизация:
- Успешный логин существующего пользователя
- Обработка попытки входа несуществующего пользователя

### Создание заказов:
- Создание заказа авторизованным пользователем
- Создание заказа без авторизации
- Работа с ингредиентами (корректные, отсутствующие, невалидные)
- Обработка ошибок сервера


## Файлы:
- allure_results - каталог с отчетом о тестировании
- data/urls - файл с URL сервиса и ручками
- helpers/helpers.py - файл с методами создания тестовых данных
- tests/test_create_order.py - файл с проверками создания заказов 
- tests/test_create_user.py - файл с проверками создания пользователя
- tests/test_login.py - файл с проверками авторизации пользователя
- requirements.txt - файл с внешними зависимостями
- conftest.py - файл с фикстурой

Перед работой с репозиторием требуется установить зависимости 
```
pip install -r requirements.txt
```
Запустить все тесты
```
pytest tests --alluredir=allure_results
```
Посмотреть отчет о тестировании
```
allure serve allure_results
```
